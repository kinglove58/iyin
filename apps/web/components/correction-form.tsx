"use client";
import { useState, type FormEvent } from "react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000/api/v1";
export function CorrectionForm(){const [state,setState]=useState<"idle"|"sending"|"done"|"error">("idle"); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=new FormData(e.currentTarget);const body={category:form.get("category"),description:form.get("description"),reporter_email:form.get("email")||null};try{const res=await fetch(`${API}/corrections`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setState(res.ok?"done":"error")}catch{setState("error")}} if(state==="done")return <div className="rounded-xl border border-emerald-300 bg-emerald-50 p-6"><h2 className="text-2xl">Request received</h2><p className="mt-2">A research administrator can now review it through the accountable correction workflow.</p></div>;return <form onSubmit={submit} className="grid gap-5 rounded-xl border border-[var(--line)] bg-[var(--card)] p-6"><label className="grid gap-2 font-bold">Issue category<select name="category" className="rounded-md border border-[var(--line)] bg-white p-3"><option value="wrong_attribution">Wrong attribution</option><option value="incorrect_date">Incorrect date</option><option value="broken_source">Broken source</option><option value="misleading_interpretation">Misleading interpretation</option><option value="duplicate">Duplicate</option><option value="copyright_concern">Copyright concern</option><option value="removal_request">Removal request</option></select></label><label className="grid gap-2 font-bold">What should we review?<textarea name="description" required minLength={20} maxLength={5000} className="min-h-40 rounded-md border border-[var(--line)] bg-white p-3"/></label><label className="grid gap-2 font-bold">Email (optional)<input name="email" type="email" className="rounded-md border border-[var(--line)] bg-white p-3"/></label>{state==="error"&&<p role="alert" className="text-red-800">The request could not be submitted. Check the API connection and try again.</p>}<button disabled={state==="sending"} className="rounded-full bg-[var(--forest)] px-5 py-3 font-bold text-white">{state==="sending"?"Submitting…":"Submit for review"}</button></form>}
