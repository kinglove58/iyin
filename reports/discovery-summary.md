# Discovery summary

No live discovery has been run. `LIVE_CRAWLING_ENABLED=false` is the default and no
Zyte or YouTube credentials are committed. When an authorized administrator runs
discovery, this report must record raw and normalized result totals, domains,
estimated types, date distribution, duplicate groups, score distribution, rejected
domains, extraction feasibility, cost, and source gaps. Candidate results remain
pending and do not enter retrieval.
